export default function AuditPage() {
  return (
    <section>
      <h1 style={{ fontSize: '1.2rem' }}>Audit Log</h1>
      <p style={{ color: '#666', fontSize: '0.85rem' }}>
        Compliance / CEO only. Read-only skeleton — wiring to the real audit trail
        (`@markaba/api` audit module) lands in a later sprint.
      </p>
    </section>
  );
}
